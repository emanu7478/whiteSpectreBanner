import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    height: 10,
    width: 10,
    borderRadius: 5,
    backgroundColor: '#fff',
    marginHorizontal: 5,
    position: 'absolute',
  },
});
